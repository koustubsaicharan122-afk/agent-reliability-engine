GEMINI_API_KEY=your-gemini-api-key-here

# Optional overrides (defaults shown):
# GEMINI_AGENT_MODEL=gemini-3.6-flash   # the "agent under test" being evaluated
# GEMINI_JUDGE_MODEL=gemini-3.6-flash   # used for scenario generation + failure classification
